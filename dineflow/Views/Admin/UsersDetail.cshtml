@using Microsoft.AspNetCore.Identity
@model List<dineflow.Models.ApplicationUser>

@{
    ViewData["Title"] = "User List";
}
<p>
    <a asp-area="Identity" asp-page="/Account/Register">Add User</a> | <a asp-controller="Admin" asp-action="Roles">Roles</a>
</p>

<div class="main p-6">
    <h2>All Users</h2>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Email</th>
                <th>Full Name</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.Email</td>
                    <td>@user.Firstname @user.Lastname</td>
                    <td>@user.Status</td>
                    <td>
                        <a class="btn btn-primary" asp-controller="Admin" asp-action="UsersDetail" asp-route-id="@user.Id">View</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
